#!/bin/bash
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------#
if [ "${TMPDIR}" == "" ];
then
	TMPDIR='/tmp/';
fi
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------#
touch "${TMPDIR}/cron_root";
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------#
TIME=$(date '+%H:%M'); # "hh:mm"
MIN=$(date '+%M'); # "mm"
DAYNAME=$(date '+%A'); # "Воскресенье"
#DAYW=$(date '+%u'); # day of week (1..7); 1 is Monday
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------#
# obsolete
#FLAG_RELOAD='1' PID_FILE='/var/run/docker_build.pid'  /usr/local/bin/daemon.sh /usr/local/bin/cron_docker_build.sh  &> /dev/null < /dev/null &
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------#
# obsolete
#FLAG_RELOAD='1' PID_FILE='/var/run/docker_deploy.pid' /usr/local/bin/daemon.sh /usr/local/bin/cron_docker_deploy.sh &> /dev/null < /dev/null &
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------#
FLAG_RELOAD='1' PID_FILE='/var/run/cron_ci_cd.pid' /usr/local/bin/daemon.sh /usr/local/bin/cron_ci_cd.sh &> /dev/null < /dev/null &
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------#
exit 0;
